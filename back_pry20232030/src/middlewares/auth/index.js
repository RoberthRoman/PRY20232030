const {
  validateLogin,
  validateRefreshToken,
  validateForgotPassword,
  validateResetPassword,
} = require('./validations');

module.exports = {
  validateLogin,
  validateRefreshToken,
  validateForgotPassword,
  validateResetPassword,
};
